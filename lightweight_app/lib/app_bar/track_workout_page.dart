import "package:flutter/material.dart";

class TrackWorkoutPage extends StatelessWidget {
  const TrackWorkoutPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('TrackWorkoutPage');
  }
}