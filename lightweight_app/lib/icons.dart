import "package:flutter/material.dart";

class MyIcons {
  Icon deleteIcon() {
    return const Icon( 
      Icons.remove_rounded,
      color: Colors.white,
      size: 30,
    );
  }

  Icon backArrowIcon() {
    return const Icon(
      Icons.arrow_back_ios_new_rounded,
      color: Colors.white,
      size: 30,
    );
  }

  Icon addIcon() {
    return const Icon(
      Icons.add_rounded,
      color: Colors.white,
      size: 30,
    );
  }

  Icon checkIcon() {
    return const Icon(
      Icons.check_rounded,
      color: Colors.white,
      size: 30,
    );
  }

  Icon editIcon() {
    return const Icon(
      Icons.check_rounded,
      color: Colors.white,
      size: 30,
    );
  }

  Icon menuIcon() {
    return const Icon(
      Icons.menu_rounded,
      color: Colors.white,
      size: 30,
    );
  }

}