import "package:flutter/material.dart";

class CurrentSplit extends StatelessWidget {
  const CurrentSplit({super.key});

    @override
  Widget build(BuildContext context) {
    //return planYourWorkoutsWidget(context);
    return Scaffold(
      appBar: AppBar(
        title: Text('My Current Split'),
      ),
    );
  }
}