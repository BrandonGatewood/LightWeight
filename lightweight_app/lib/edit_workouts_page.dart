import "package:flutter/material.dart";

class EditWorkoutsPage extends StatelessWidget {
  const EditWorkoutsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('SettingsPage');
  }
}